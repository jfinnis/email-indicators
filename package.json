{
  "name": "email-indicators",
  "version": "1.0.0",
  "description": "Email unread count indicators for tmux",
  "type": "module",
  "scripts": {
    "start": "bun run src/email-counts.ts",
    "lint": "biome check --write src/",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}